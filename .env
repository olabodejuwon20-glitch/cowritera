# WARNING: Secrets removed
# This file previously contained live credentials. They have been redacted for security.
#
# Do NOT commit real secrets to the repository. Provide runtime secrets via your hosting
# provider's environment variable settings (Vercel, Netlify, Render, Supabase, etc.).
#
# Example values are in .env.example. After rotating keys, set them in your host and
# never commit them to source control.

# Placeholder values (do not use in production)
SUPABASE_PROJECT_ID="REDACTED"
SUPABASE_PUBLISHABLE_KEY="REDACTED"
SUPABASE_URL="https://REDACTED.supabase.co"
VITE_SUPABASE_PROJECT_ID="REDACTED"
VITE_SUPABASE_PUBLISHABLE_KEY="REDACTED"
VITE_SUPABASE_URL="https://REDACTED.supabase.co"
